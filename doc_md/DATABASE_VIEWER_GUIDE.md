# 🛠️ Database Viewer - คู่มือการใช้งาน

## การเข้าถึง

### วิธีที่ 1: ผ่าน Sidebar Menu
1. เปิดแอป
2. คลิก **DevTools** ใน sidebar
3. เลือก **Database Viewer**

### วิธีที่ 2: URL โดยตรง
```
http://localhost:5173/devtools
```

---

## ⚡ Features

### 📊 Database Quick View
แสดงภาพรวมของ database ทั้งหมด:
- **จำนวนรายการ** ในแต่ละ table
- **สถิติรวม** (Total items, Tables count)
- **พื้นที่ใช้งาน** (Storage usage)

### 🔍 Database Viewer
จัดการข้อมูลแบบละเอียด:
- **ดูข้อมูล** ทุก table
- **ค้นหา** ข้อมูล
- **ลบข้อมูล** รายการเดียวหรือทั้ง table
- **Export** ข้อมูลเป็น JSON
- **View Details** ดูข้อมูลแบบ JSON formatted

---

## 🎯 การใช้งาน

### ดูข้อมูลใน Table
1. เลือก **Tab** ของ table ที่ต้องการดู
2. ข้อมูลจะแสดงเป็น cards พร้อม key-value pairs

### ค้นหาข้อมูล
1. พิมพ์คำค้นหาใน **Search box**
2. ระบบจะ filter แสดงเฉพาะข้อมูลที่ตรงกัน

### ดูรายละเอียด
1. คลิกปุ่ม **👁️ View** ที่รายการ
2. จะเปิด Modal แสดง JSON formatted

### ลบข้อมูล
- **ลบรายการเดียว**: คลิก **🗑️ Delete** ที่รายการ
- **ลบทั้ง table**: คลิก **🗑️ Clear Table** ด้านบน

### Export ข้อมูล
1. เลือก table ที่ต้องการ export
2. คลิก **📥 Export**
3. ไฟล์ JSON จะถูก download

### Refresh ข้อมูล
คลิก **🔄 Refresh** เพื่ออัพเดทข้อมูลล่าสุด

---

## 📋 Tables ที่มี

| Table | Description |
|-------|-------------|
| **config** | การตั้งค่าทั่วไป (theme, launcher, onboarding) |
| **appState** | สถานะของแต่ละ app (favorite, installed) |
| **todos** | รายการ todos (ถ้ามี) |
| **notes** | รายการ notes (ถ้ามี) |
| **userProfiles** | โปรไฟล์ผู้ใช้ (ถ้ามี) |

---

## 🔒 Security Note

**⚠️ DevTools นี้ควรใช้เฉพาะใน Development เท่านั้น!**

สำหรับ production ควร:
1. ซ่อน menu DevTools
2. ปิด route `/devtools`
3. ใช้ feature flag เพื่อเปิด/ปิด

### วิธีปิด DevTools (สำหรับ Production)

```typescript
// ใน App.tsx
const isDev = import.meta.env.DEV;

// ใน baseMenuItems
const baseMenuItems: MenuItem[] = [
  // ... other items ...
  ...(isDev ? [{ // เพิ่ม DevTools ก็ต่อเมื่ออยู่ใน dev mode
    id: "devtools",
    label: "DevTools",
    icon: <Wrench size={18} />,
    basePath: "/devtools",
    subItems: [
      { label: "Database Viewer", path: "/devtools" },
    ],
  }] : []),
];
```

---

## 🎨 Screenshots

### Quick View
แสดงสถิติรวมของทุก table

### Full Viewer
- Tabs สำหรับเลือก table
- Search box
- Cards แสดงข้อมูล
- Action buttons (View, Delete)

---

## 💡 Tips

1. **ใช้ Search** เพื่อหาข้อมูลเร็วขึ้น
2. **Export ก่อนลบ** เพื่อ backup ข้อมูล
3. **Refresh บ่อยๆ** ถ้ามีการแก้ไขข้อมูลจากที่อื่น
4. **ดู Quick View** ก่อนเสมอเพื่อเช็คภาพรวม

---

## 🐛 Troubleshooting

### ไม่เห็น menu DevTools
- ตรวจสอบว่า import และ route ถูกต้อง
- Refresh browser

### ข้อมูลไม่แสดง
- คลิก Refresh button
- เช็ค browser console สำหรับ errors
- ตรวจสอบว่ามี IndexedDB ถูกสร้างแล้ว

### Export ไม่ทำงาน
- ตรวจสอบ browser settings สำหรับ download permissions
- ลองใน browser อื่น

---

## 🔗 Related

- [CRUD_GUIDE.md](../modules/storage/CRUD_GUIDE.md) - คู่มือ CRUD operations
- [QUICK_START.md](../modules/storage/QUICK_START.md) - Quick start guide
- [GenericCrudDemo.tsx](../examples/GenericCrudDemo.tsx) - ตัวอย่าง components
